@foreach (var car in Model)
{
    <tr>
        @if (car.ShowAsFast)
        {
            <th style="color: Highlight; font-style: italic">@car.Brand</th>
        }
        else
        {
            <th>@car.Brand</th>
        }
    </tr>
}